<x-seller-layout title="Detail Produk">
    <div class="rounded-xl bg-white p-6 shadow-sm sm:p-8">
        <div class="flex flex-col justify-between gap-4 sm:flex-row sm:items-start"><div><div class="flex items-center gap-3"><h3 class="text-xl font-semibold text-gray-900">{{ $product->name }}</h3><x-product-status-badge :status="$product->status" /></div><p class="mt-2 text-sm text-gray-500">{{ $product->category->name }} · Dibuat {{ $product->created_at->format('d M Y') }}</p></div><a href="{{ route('seller.products.edit', $product) }}" class="rounded-lg bg-indigo-600 px-4 py-2 text-sm font-semibold text-white hover:bg-indigo-500">{{ $product->status === 'rejected' ? 'Perbaiki Produk' : 'Edit Produk' }}</a></div>
        @if ($product->status === 'rejected')<div class="mt-6 rounded-lg border border-rose-200 bg-rose-50 p-4 text-sm text-rose-800"><p class="font-semibold">Produk ditolak</p><p class="mt-1">{{ $product->rejection_reason ?: 'Admin belum memberikan alasan penolakan.' }}</p></div>@endif
        <div class="mt-8 grid gap-8 md:grid-cols-3"><div>@if ($product->image)<img src="{{ Storage::disk('public')->url($product->image) }}" alt="{{ $product->name }}" class="aspect-square w-full rounded-xl object-cover">@else<div class="flex aspect-square items-center justify-center rounded-xl bg-indigo-50 text-sm font-semibold text-indigo-600">Belum ada foto</div>@endif</div><div class="md:col-span-2"><dl class="grid gap-5 sm:grid-cols-2"><div><dt class="text-xs font-medium uppercase tracking-wide text-gray-500">Harga</dt><dd class="mt-1 text-lg font-semibold text-gray-900">Rp{{ number_format($product->price, 0, ',', '.') }}</dd></div><div><dt class="text-xs font-medium uppercase tracking-wide text-gray-500">Stok</dt><dd class="mt-1 text-lg font-semibold text-gray-900">{{ $product->stock }}</dd></div><div class="sm:col-span-2"><dt class="text-xs font-medium uppercase tracking-wide text-gray-500">Deskripsi</dt><dd class="mt-1 whitespace-pre-line text-sm leading-6 text-gray-700">{{ $product->description }}</dd></div></dl></div></div>
    </div>
</x-seller-layout>
